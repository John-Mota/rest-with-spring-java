spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_with_spring_form?useTimezone=true&serverTimezone=UTC
    username: root
    password: john3472
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      '[hibernate.dialect]': org.hibernate.dialect.MySQLDialect
      open-in-view: false
    show-sql: false